const initialState = {
  selectedDay: null,
};

const ActionType = {
  
};

export const ActionCreator = {
  
};


export const Operation = {

}

export function reducer(state = initialState , action) {
  switch (action.type) {

    default:
      return state;
  }
};